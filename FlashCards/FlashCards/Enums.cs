enum MainMenuOption
{
    ManageStacks,
    ManageFlashCards,
    Study,
    ViewStudySessions,
    GetReport,
    Exit,
}

enum StackMenuOption
{
    ViewAllStacks,
    CreateNewStack,
    RenameStack,
    DeleteStack,
    ReturnToMainMenu
}
enum FlashCardMenuOption
{
    ViewAllCards,
    ViewXCards,
    CreateNewFlashCard,
    UpdateFlashCard,
    DeleteFlashCard,
    SwitchStack,
    ReturnToMainMenu
}
enum PivotFunction
{
    Average,
    Count,
    Sum
}
