<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <appSettings>
    <!-- Please replace the connection strings with your own SQL Server instance IP, database name and credentials -->
    <!-- Database must exist, although it can have no tables present -->
    <add key="ConnectionString" value="Server=192.168.1.57;Database=FLASHCARD;Trusted_Connection=True;TrustServerCertificate=True;User Id=flashcarduser;Password=!!flashcard36;Integrated Security=False;"/>
  </appSettings>
</configuration>