namespace DotNETConsole.Flashcards.Enums;

public enum MainUI
{
    StartStudySession,
    ViewSessionLog,
    CreateStack,
    ViewStack,
    AddCards,
    ViewCards,
    ModifyStack,
    ModifyCard,
    Reset,
    Exit
}

public enum ModifyActions
{
    Edit,
    Delete
}
