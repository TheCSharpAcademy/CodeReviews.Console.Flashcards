namespace DotNETConsole.Flashcards.Enums;

public enum MainUI
{
    StartStudySession,
    ViewSessionLog,
    CreateStack,
    ViewStack,
    ViewCards,
    AddCards,
    ModifyStack,
    ModifyCard,
    Exit
}

public enum ModifyActions
{
    Edit,
    Delete
}
